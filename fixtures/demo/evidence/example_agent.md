# Example Agent Project (synthetic evidence)

This file is a synthetic stand-in for a real project evidence note. A real
note would contain build logs, repo links, or dated work records. The gate
only requires that the path resolves; its content is the human's review job,
not the machine's.

- Built a local agent workflow with tool wiring and written definitions of done.
- Demo scheduler shipped with a CLI and unit tests.
