# Optional connector lanes (network reads; see docs/AGENT_CONTRACT.md).
google-api-python-client>=2.170.0
google-auth-oauthlib>=1.2.2
PySocks>=1.7.1
# Notion lane additionally requires a local Hermes runtime providing
# `mcp` and `tools.mcp_oauth_manager`; it cannot run from this mirror alone.
